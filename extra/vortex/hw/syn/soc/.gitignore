# src/, sources.txt, grlib/svlogsyn.txt are pre-generated on the VM and
# committed to this repo so the sim machine does not need Verilator.
# Only Verilator scratch (obj_dir/) stays ignored.
obj_dir/
